cd /
cd /Users/selva/git/docker-demo
docker stop helloworld
docker rm helloworld
docker build -t hello-world /Users/selva
docker run -d --name helloworld -p 80:80 -v /users/selva/docker-demo/src/:/var/wwe/html hello-world
